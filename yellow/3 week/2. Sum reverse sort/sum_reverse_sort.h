#ifndef _SUM_REVERSE_SORT_H_
#define _SUM_REVERSE_SORT_H_

#include <string>
#include <vector>

using namespace std;

int Sum(int x, int y);
string Reverse(string s);
void Sort(vector<int>& nums);

#endif